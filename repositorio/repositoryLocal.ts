import { Local } from '../classes/local'

class repositoryLocal {
    private connection = { execute(query: string) {} };

    insert(){
    }
    getAll(){
    }
    getById(){
    }
    delete(local: Local) {
    }
    update(local: Local){

    }
  }
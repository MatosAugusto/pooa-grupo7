import { Etapa } from '../classes/etapa'

class repositoryEtapa {
    private connection = { execute(query: string) {} };

    insert(){
    }
    getAll(){
    }
    getById(){
    }
    delete(etapa: Etapa) {
    }
    update(etapa: Etapa){

    }
  }
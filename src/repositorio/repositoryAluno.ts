import { Aluno } from '../classes/aluno'

export class repositoryAluno {
    private connection = { execute(query: string) {} };

    insert(){
    }
    getAll(){
    }
    getById(){
    }
    delete(aluno: Aluno) {
    }
    update(aluno: Aluno){

    }
  }
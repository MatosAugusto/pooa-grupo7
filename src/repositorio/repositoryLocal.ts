import { Local } from '../classes/local'

export class repositoryLocal {
    private connection = { execute(query: string) {} };

    insert(){
    }
    getAll(){
    }
    getById(){
    }
    delete(local: Local) {
    }
    update(local: Local){

    }
  }